spring:
  application:
    name: beer-service

---
spring:
  profiles: local-discovery
  cloud:
    config:
      discovery:
        service-id: brewery-config-server
        enabled: true
      fail-fast: true